:root{
  --bg:#0b0b0b;
  --panel:#0f1720;
  --muted:#9aa5b1;
  --accent:#00e6ff;
  --glass: rgba(255,255,255,0.03);
  --card-radius:14px;
  --gap:14px;
}
*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family:Inter,system-ui,Segoe UI,Roboto, "Helvetica Neue", Arial;
  background:linear-gradient(180deg,#050506 0%, #0b0b0b 100%);
  color:#dbe9ee;
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
}
.topbar{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:12px 20px;
  background:linear-gradient(90deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
  border-bottom:1px solid rgba(255,255,255,0.03);
}
.brand{display:flex;gap:10px;align-items:center}
.logo{width:68px;height:68px;object-fit:contain;border-radius:10px;background:var(--glass);padding:6px}
.brand h1{margin:0;font-size:20px;letter-spacing:1px}
.tag{margin:0;font-size:12px;color:var(--muted)}
.nav{display:flex;gap:8px;align-items:center}
.nav-btn{
  background:transparent;border:1px solid rgba(255,255,255,0.03);padding:8px 12px;border-radius:8px;color:var(--muted);cursor:pointer;
}
.nav-btn.active, .nav-btn:hover{border-color:var(--accent);color:var(--accent)}

.main{padding:28px;max-width:1100px;margin:0 auto;min-height:70vh}
.tab-content{display:none}
.tab-content.visible{display:block}
.hero{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));padding:36px;border-radius:12px}
.hero h2{margin:0 0 6px 0;font-size:28px}
.hero p{margin:0 0 12px 0;color:var(--muted)}
.cta{display:inline-block;padding:10px 16px;border-radius:10px;border:1px solid var(--accent);color:var(--accent);text-decoration:none;cursor:pointer}

.cards{display:flex;gap:12px;flex-wrap:wrap;margin-top:12px}
.card{background:var(--panel);padding:18px;border-radius:12px;min-width:220px;flex:1;box-shadow:0 6px 18px rgba(2,6,23,0.6)}

.team-grid{display:flex;gap:12px;flex-wrap:wrap;margin-top:12px}
.member{
  background:linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.01));
  padding:12px;border-radius:10px;width:220px;position:relative;border:1px solid rgba(255,255,255,0.02)
}
.member h4{margin:0 0 6px 0}
.tagrole{font-size:12px;color:var(--muted)}

.admin-actions{margin-top:18px;background:var(--panel);padding:12px;border-radius:10px}
.form-row{display:flex;gap:8px}
.form-row input,.form-row select{flex:1;padding:8px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}

.small-form{display:flex;gap:8px;margin-top:12px}
.small-form input{padding:8px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}

.events-list{list-style:none;padding:0;margin:8px 0}
.events-list li{padding:8px;border-radius:8px;background:rgba(255,255,255,0.02);margin-bottom:8px}

.login-card{max-width:420px;background:var(--panel);padding:16px;border-radius:10px}
.login-card input{width:100%;padding:8px;margin-bottom:8px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}

.hidden{display:none}
.footer{padding:16px;text-align:center;color:var(--muted)}

.note{font-size:12px;color:var(--muted);margin-top:8px}

.secret-area{display:flex;gap:12px;flex-wrap:wrap;margin-top:12px}
.secret-card{flex:1;min-width:320px;background:rgba(255,255,255,0.03);padding:12px;border-radius:10px}
.secret-card textarea{width:100%;height:120px;padding:8px;border-radius:8px;background:transparent;color:inherit;border:1px solid rgba(255,255,255,0.04)}
.file-list{margin-top:8px}
.row{display:flex;gap:8px}
button{cursor:pointer;padding:8px 12px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}
button.primary{background:var(--accent);color:#001; border:none}
